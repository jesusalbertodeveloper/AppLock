<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".AccServDisableError">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar6999"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        app:title="Privacy Policy rejected"
        app:titleTextColor="#FFFFFF" />

    <TextView
        android:id="@+id/textView5999"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You have rejected the Privacy Policy.NO changes have been made to your device.If you don't want to use this app just uninstall it"
        android:textSize="24sp" />

    <Button
        android:id="@+id/button922999"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="OK" />
</LinearLayout>